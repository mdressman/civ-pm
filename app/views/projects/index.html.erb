<% provide(:title, 'All Projects') %>
<h1>All Projects <span class="small-link"> | <%= link_to "New Project", new_project_path %></span></h1>

<%= will_paginate %>

<ul class="projects">
	<%= render @projects %>
</ul>

<%= will_paginate %>