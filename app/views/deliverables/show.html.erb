<% provide(:title, @deliverable.name) %>
<div class="row">
  
  <div class="span12">
  	<section>
  		<h1><%= @deliverable.name %></h1>

  		
      <p>
          <%= link_to "edit", edit_client_project_deliverable_path(@client.id, @deliverable.project_id, @deliverable) %> | 
          <%= link_to "delete", client_project_deliverable_path(@client.id, @deliverable.project_id, @deliverable),method: :delete, data: { confirm: "You sure?" }, title: @deliverable.name %>
      </p>


  		<p>Client: <%= Client.find_by(@project.client_id).name %></p>
      <p>Project: <%= @project.name %></p>
  		<% if @deliverable.completed_date? %><p>Completed Date: <%= @deliverable.completed_date.strftime('%m/%d/%Y') %></p> <% end %>
      <p>Received?: <%= @deliverable.complete? %></p>
      <p>Link: <%= @deliverable.link %></p>
  	</section>
    
  </div>
</div>